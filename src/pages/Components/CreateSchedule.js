import React from 'react';
import { Button } from 'semantic-ui-react';

const CreateSchedule = () => (
  <Button 
  onClick={() => console.log('Schedule has been created!')}>Create Schedule
  </Button>
);

export default CreateSchedule 